<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bedspaceex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bedspace_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bed2"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity"></activity>
        <activity android:name=".Activities.RegisterActivity" />
        <activity
            android:name=".Activities.InsertBedSpaceDealActivity"
            android:label="Upload Your Offer"
            android:parentActivityName=".Activities.BedSpaceListActivity" />
        <activity
            android:name=".Activities.BedSpaceActivity"
            android:label="Room Details"
            android:parentActivityName=".Activities.BedSpaceListActivity" />
        <activity
            android:name=".Activities.BedSpaceListActivity" />
    </application>

</manifest>